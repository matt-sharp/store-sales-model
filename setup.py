from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A model designed to predict the future sales of a store based on location characteristics.',
    author='Matthew Sharp',
    license='MIT',
)
